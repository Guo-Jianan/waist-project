2026-05-13 | WH

- **重大架构变更：ESP01S从TCP Server改为MQTT Client**
  - ESP01S工作模式从AP+TCP Server改为STA+MQTT Client
  - 通过broker.emqx.io:1883（MQTT over TCP）连接公共Broker
  - 订阅主题 `waist/device001/cmd`（QoS 1）接收JSON控制指令
  - 指令格式：`{"cmd":"set_force","device_id":"device001","RB":0.0,"RF":0.0,"LB":61.0,"LF":42.0}`
  - JSON字段解析兼容引号转义与裸JSON两种格式
- **driver_ESP01s 新增MQTT功能函数**
  - `ESP8266_ConnectAP()` — STA模式连接WiFi
  - `ESP8266_ConnectMQTT()` — MQTT用户配置+Broker连接，带3次重试
  - `ESP8266_Subscribe()` — 订阅MQTT主题
  - `ESP8266_MQTT_HandleReceivedLine()` — 解析+MQTTSUBRECV行，提取JSON填充ActuatorTarget
- **freertos.c 重构StartDefaultTask**
  - 删除原有ESP8266_InitAP和ESP8266_CreateTCPServer调用
  - 新增WiFi连接→MQTT连接→MQTT订阅初始化流程
  - 主循环改为轮询mqtt_line_ready标志处理MQTT消息
  - defaultTask栈大小从128×4增大到512×4
- **UART接收机制修复**
  - 恢复中断式AT指令收发（ESP8266_SendATCommand），解决HAL轮询收不到数据
  - 空闲中断增加MQTT消息缓存（ISR轻量拷贝→任务消费）
  - 空闲中断处理完毕后清rx_len，修复缓冲区满后重复解析旧数据
  - ESP8266_SendATCommand超时路径增加cmd_state=CMD_IDLE，防止ISR永久阻塞MQTT缓存
  - WiFi连接后延时5000ms等待DHCP获取IP
- **ESP8266_Device结构体扩展**
  - 新增 `mqtt_line_ready` / `mqtt_line_buf[512]` 用于MQTT消息缓存

---

2026-03-24 | WH

- 调整ADC配置为4通道：PC1(LF)、PC2(LB)、PA2(RF)、PA3(RB)
- 调整PWM输出引脚，LF/LB使用TIM5 CH1-4 (PF6-PF9)
- 完成四路推杆(RB/RF/LF/LB)硬件配置
- 软件层完成四路设备对象、PID初始化、闭环控制逻辑
- Shell新增RF/LF/LB命令支持（增量模式）
- 启用FreeRTOS Queue Sets功能
- ESP8266通信接口从UART4(PA0/PA1)改为USART1(PG9/PG10)
